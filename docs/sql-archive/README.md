# SQL archive

One-shot SQL files that were applied to the database directly
before the formal supabase/migrations/ system existed. Kept for
historical reference only — every statement here has already been
applied to the production database. **Do not re-run.**

New schema changes belong in supabase/migrations/.
