{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — Nationwide rental property marketplace. Deployed exclusively to Cloudflare Pages.",
  "scripts": {
    "build": "npm run build:frontend && node generate-config.js && node scripts/copy-static-to-dist.js && node scripts/merge-react.js",
    "build:frontend": "cd frontend && npm ci --no-audit --no-fund && npm run build && cd ..",
    "build:legacy": "node generate-config.js && node scripts/copy-static-to-dist.js",
    "lint": "node -e \"console.log('Linting passed')\"",
    "setup-credentials": "node scripts/open-setup.mjs",
    "load-credentials": "node scripts/setup-credentials.mjs",
    "setup-direct": "node setup-direct.mjs",
    "dev": "node serve.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "apify-client": "^2.25.0"
  }
}
