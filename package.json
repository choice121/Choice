{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — Nationwide rental property marketplace.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node generate-config.js",
    "lint": "node -c server.js generate-config.js",
    "setup-credentials": "node scripts/open-setup.mjs",
    "load-credentials": "node scripts/setup-credentials.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
