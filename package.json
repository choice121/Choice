{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — Cloudflare Pages static site with Supabase backend. Replit is editing-only and must not run, migrate, or host this project.",
  "scripts": {
    "preinstall": "node scripts/enforce-cloudflare-only.js install",
    "build": "node scripts/enforce-cloudflare-only.js build && node generate-config.js",
    "verify:cloudflare-only": "node scripts/enforce-cloudflare-only.js build"
  },
  "engines": {
    "node": ">=18"
  }
}
