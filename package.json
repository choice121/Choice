{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — Nationwide rental property marketplace. Served on Replit, deployed to Cloudflare Pages.",
  "scripts": {
    "start": "node scripts/generate-config-replit.js && node server.js",
    "build": "node generate-config.js",
    "dev": "node scripts/generate-config-replit.js && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
