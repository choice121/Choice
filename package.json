{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — rental marketplace adapted for Replit with a Node server and PostgreSQL backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node generate-config.js",
    "db:push": "node scripts/db-push.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
