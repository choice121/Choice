{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Choice Properties — Nationwide rental property marketplace. Deployed exclusively to Cloudflare Pages.",
  "scripts": {
    "build": "npm run build:frontend && node scripts/copy-static-to-dist.js && node generate-config.js && node scripts/merge-react.js",
    "build:frontend": "cd frontend && npm ci --include=dev --no-audit --no-fund && npm run build && cd ..",
    "build:legacy": "node scripts/copy-static-to-dist.js && node generate-config.js",
    "lint": "cd frontend && npm ci --include=dev --no-audit --no-fund && npm run lint",
    "setup-credentials": "node scripts/open-setup.mjs",
    "load-credentials": "node scripts/setup-credentials.mjs",
    "setup-direct": "node setup-direct.mjs",
    "dev": "node serve.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "apify-client": "^2.25.0"
  }
}
